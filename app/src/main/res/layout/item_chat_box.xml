<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="0dp"
    android:id="@+id/layout_all"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:clickable="true"
    android:layout_marginBottom="8dp"
    android:layout_marginTop="0dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tvTime"
        android:text="20:10, 20/03/2021"
        style="@style/TextViewTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="18dp"
        android:layout_marginTop="6dp"
        >
        <androidx.cardview.widget.CardView
            android:id="@+id/cover"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="150sp"
            android:layout_alignParentLeft="true"
            android:foreground="@drawable/rounded_image"
            >
            <ImageView
                android:id="@+id/ivAvatar"
                android:src="@drawable/default_avatar"
                android:scaleType="fitCenter"
                android:layout_width="28dp"
                android:layout_height="28dp"
                style="@style/AvatarSmallImage"/>
        </androidx.cardview.widget.CardView>
        <LinearLayout
            android:id="@+id/layout_content"
            android:layout_toRightOf="@id/cover"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="16dp"
            android:orientation="vertical">
            <include
                layout="@layout/text_content_layout"/>
            <include
                layout="@layout/img_content_layout"/>
        </LinearLayout>
    </RelativeLayout>

</LinearLayout>